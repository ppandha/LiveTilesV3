@model IEnumerable<LiveTilesV3.Models.Twitter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SearchCriteria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SearchCriteria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TwitterId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TwitterId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TwitterId })
        </td>
    </tr>
}

</table>
