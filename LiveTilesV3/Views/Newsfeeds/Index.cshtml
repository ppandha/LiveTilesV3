@model IEnumerable<LiveTilesV3.Models.Newsfeed>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RssUrl)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RssUrl)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NewsfeedId }) |
            @Html.ActionLink("Details", "Details", new { id=item.NewsfeedId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NewsfeedId })
        </td>
    </tr>
}

</table>
